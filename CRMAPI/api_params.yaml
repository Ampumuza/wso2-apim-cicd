environments:
  - name: test
    endpoints:
      production:
        url: http://localhost:8595/
        config:
          retryTimeOut: $RETRY
      sandbox:
        url: http://localhost:8595/
        config:
          retryTimeOut: $RETRY        
  - name: prod
    endpoints:
      production:
        url: http://localhost:8595/
        config:
          retryTimeOut: $RETRY      
      sandbox:
        url: http://localhost:8595/
        config:
          retryTimeOut: $RETRY 
